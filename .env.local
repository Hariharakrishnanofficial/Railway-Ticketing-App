# LOCAL DEV ONLY — Vite auto-ignores this file in production builds.
# Leave blank so axios uses '/api/' and Vite proxy forwards to Flask.
VITE_API_BASE_URL=http://localhost:3002/api/
VITE_ENV=development
